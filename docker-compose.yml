version: '3.8'

services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: realnext-backend
    ports:
      - "5078:5000"
    environment:
      - PORT=5000
      - DB_HOST=i4o0448ggkw4sskggc084ok4
      - DB_PORT=5432
      - DB_NAME=postgres
      - DB_USER=postgres
      - DB_PASSWORD=rr5inCRtzjDCFOQUkv2vrE0WDlnAWhSGhYGq30mxJWeLJYCG9oMVL0L39UjR2FTt
      - FRONTEND_URL=https://test.niveshsarthi.com
      - WHATSAPP_API_URL=https://ckk4swcsssos844w0ccos4og.72.61.248.175.sslip.io
      - SUPER_ADMIN_EMAIL=admin@realnext.com
      - SUPER_ADMIN_PASSWORD=realnextAdmin2024!debug
      - SYNC_DB=true
    restart: unless-stopped

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      args:
        NEXT_PUBLIC_API_URL: https://testbd.niveshsarthi.com
    container_name: realnext-frontend
    ports:
      - "3000:3000"
    environment:
      - NEXT_PUBLIC_API_URL=https://testbd.niveshsarthi.com
      - NODE_ENV=production
      - PORT=3000
    restart: unless-stopped
    depends_on:
      - backend
    networks:
      - coolify

networks:
  coolify:
    external: true
    networks:
      - coolify

networks:
  coolify:
    external: true
